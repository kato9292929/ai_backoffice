{
  "name": "stripe-approvals-sink",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Durable sink for Stripe Approvals async lifecycle events, for a qm org layer. Records approval_request state transitions that happen out of band (after human approval), which qm's turn-scoped transcript does not capture.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node --env-file-if-exists=.env src/index.ts"
  },
  "dependencies": {
    "pg": "^8.13.1",
    "stripe": "^17.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.11.10",
    "typescript": "^5.7.2"
  }
}
